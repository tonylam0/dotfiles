# JankyBorders
# Subtle grey for active, nothing for inactive
borders active_color=0xff928374 inactive_color=0x00000000 width=0.0 &

# This reserves 35px at the top for simple-bar.
# Format: external_bar <main|all|off>:<top_padding>:<bottom_padding>
# yabai -m config external_bar all:35:0
yabai -m config external_bar all:0:0

# bsp = Binary Space Partitioning (standard tiling)
yabai -m config layout bsp

# Gaps between windows (match your Kitty padding for aesthetics)
yabai -m config window_gap 5
yabai -m config top_padding 5
yabai -m config bottom_padding 5
yabai -m config left_padding 5
yabai -m config right_padding 5

# Focus follows mouse (optional, but recommended for efficiency)
yabai -m config focus_follows_mouse autofocus

# New window spawns to the right/bottom of the active window
yabai -m config window_placement second_child

echo "yabai configuration loaded.."

# 'manage=off' tells yabai: "Don't touch this window, let macOS handle it."
yabai -m rule --add app="^System (Settings|Preferences)$" manage=off
yabai -m rule --add app="^Messages$" manage=off
yabai -m rule --add app="^Finder$" manage=off
yabai -m rule --add app="^Cold Turkey Blocker$" manage=off
